# FLib

核心库, 主要是包含很多工具方法

# FLib.ConfigBuilder

配置表编译库, 将excel, json, json5, toml等等各种格式编译到二进制文件, app运行时加载这个编译好的二进制文件

# FLib.Gen

源码生成工具, 目前主要作用是替代protobuf直接在编译阶段就根据struct/class生成序列化方法, 不在需求.proto文件

# FLib.Tests

正在编写中的单元测试, 还在补全中....